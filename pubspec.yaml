name: mesh_alert
publish_to: 'none'
description: Offline mesh communication disaster app
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  nearby_connections: ^4.0.0
  sqflite: ^2.3.0
  path: ^1.8.3
  uuid: ^4.0.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1
  geolocator: ^13.0.0
  flutter_compass: ^0.8.0
  image_picker: ^1.0.7
  share_plus: ^7.2.1
  telephony: ^0.2.0
  connectivity_plus: ^5.0.0
  http: ^1.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/maps/
