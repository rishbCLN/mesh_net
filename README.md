# MeshAlert

Offline mesh communication disaster app for Android (Flutter)

## Features
- Offline mesh communication (no internet, no Firebase)
- Android only (minSdkVersion 21)
- Stubs only, no logic yet

## Getting Started
1. Ensure you have Flutter installed.
2. Run `flutter pub get` to install dependencies.
3. Run `flutter run` on an Android emulator or device.

## Project Structure
- `lib/` — Main Dart source files
- `android/` — Android platform files

## Dependencies
- flutter
- nearby_connections
- sqflite
- path
- uuid
- provider

## License
MIT
